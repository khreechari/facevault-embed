# Copy to .env and fill in. Used by the server examples (FastAPI / Express).
# Get these from the FaceVault dashboard: https://devdash.facevault.id

FACEVAULT_API_KEY=fv_live_your_api_key
FACEVAULT_SITE_ID=fvs_pk_your_site_id

# Optional overrides (default to production):
# FACEVAULT_API_BASE=https://api.facevault.id/api/v1
# FACEVAULT_EMBED_SRC=https://app.facevault.id/embed.js
